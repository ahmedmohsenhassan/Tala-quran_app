import 'package:flutter/material.dart';

/// ألوان التطبيق الموحدة
/// Unified app color constants
class AppColors {
  static const Color background = Color(0xFF0B1F0E);
  static const Color cardBackground = Color(0xFF1A3A1E);
  static const Color gold = Color(0xFFD4AF37);
  static const Color goldLight = Color(0xFFE8C84A);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.grey;
  static const Color divider = Color(0xFF1A3A1E);
  static const Color accent = Color(0xFF2E7D32);
}
